{
  "name": "workspace",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "sass styles.scss styles.css --no-source-map",
    "watch:css": "sass styles.scss styles.css --watch --no-source-map"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.99.0"
  }
}
